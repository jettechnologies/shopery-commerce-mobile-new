import { SafeScreen } from "@/components/safe-screen";
import { Text, View } from "react-native";

const Login = () => {
  return (
    <SafeScreen>
      <View>
        <Text>Login</Text>
      </View>
    </SafeScreen>
  );
};

export default Login;
