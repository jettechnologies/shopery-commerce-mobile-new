import { SafeScreen } from "@/components/safe-screen";
import { Text, View } from "react-native";

const Order = () => {
  return (
    <SafeScreen>
      <View>
        <Text>order</Text>
      </View>
    </SafeScreen>
  );
};

export default Order;
